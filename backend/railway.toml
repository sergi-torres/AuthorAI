# Do not use this file for deploy.
#
# Railway Root Directory MUST be empty (monorepo root). If it is set to
# `backend/`, only this folder is built — `ai_pipeline/` is missing and
# passport verify cannot import `autoria_ai`.
#
# Use the repo-root `railway.toml` instead.

[build]
builder = "NIXPACKS"
